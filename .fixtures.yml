fixtures:
  forge_modules:
    stdlib:
      repo: "puppetlabs/stdlib"
      ref: "5.1.0"
    transition:
      repo: "puppetlabs/transition"
    inifile:
      repo: "puppetlabs/inifile"
      ref: "2.4.0"
    apt:
      repo: "puppetlabs/apt"
      ref: "6.2.1"
  symlinks:
    puppet_agent: "#{source_dir}"
